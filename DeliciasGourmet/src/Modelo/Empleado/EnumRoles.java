package Modelo.Empleado;

/**
 * Enumeración que representa los diferentes roles que puede tener un empleado
 * en el sistema.
 */
public enum EnumRoles {
    /** Rol de administrador. */
    ADMIN,
    
    /** Rol de recepcionista. */
    RECEPCION,
    
    /** Rol de maitre. */
    MAITRE,
    
    /** Rol de mesero. */
    MESERO;
}
